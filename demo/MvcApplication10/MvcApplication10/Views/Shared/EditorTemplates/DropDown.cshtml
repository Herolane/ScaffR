@using MvcApplication10.Controls.DropDown.Extensions

@model object

@{
    IDictionary<string, object> Attr = new Dictionary<string, object>();

    var x = ViewData.ModelMetadata
                    .BuildDropdownListenerAttributes(ViewData.TemplateInfo, ref Attr)
                    .BuildDropdownListenerWithSpecificValue(ViewData.TemplateInfo, ref Attr);     
}

@Html.DropDownFor(m => m, Model, Attr)